<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <permission
        android:name="sp.cisco.permission.GETTER_INTENT"
        android:label="my_permission"
        android:protectionLevel="signature" />

    <uses-permission android:name="sp.cisco.permission.GETTER_INTENT" />

    <queries>

        <!-- Cisco Anyconnect package name -->
        <package android:name="com.cisco.anyconnect.vpn.android.avf" />
        <package android:name="android.permission.QUERY_ALL_PACKAGES" />

        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication2"
        tools:targetApi="31">
        <activity
            android:name=".ScrollingActivity"
            android:exported="false"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/Theme.MyApplication2.NoActionBar" />
        <activity
            android:name=".TestActivity"
            android:theme="@style/Theme.Transparent.Me"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".broadcasts.ReceiverGetter"
            android:enabled="true"
            android:exported="true"
            android:permission="sp.cisco.permission.GETTER_INTENT">
            <intent-filter>
                <action android:name="sp.cisco.GETTER_INTENT" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
    </application>

</manifest>